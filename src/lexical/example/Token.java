package org.example;

public class Token {
    public String token;
    public TipoToken type;

    public Token(String token, TipoToken type) {
        this.token = token;
        this.type = type;
    }
}
